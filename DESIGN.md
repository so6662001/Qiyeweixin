# 企业微信机器人 — 设计文档（v2）

> 状态：设计阶段（尚未开发）
> 行业：**钢铁贸易**
> 目标：搭建一个能接收用户消息、调用业务 API 查询数据、再把结果回复给用户的企业微信机器人；同时支持内部员工和外部微信用户；接入 LLM（DeepSeek / 通义千问）做意图理解与多轮对话。

---

## 0. 需求方已确认的关键约束（v2 新增）

| # | 问题 | 答复 | 对设计的影响 |
|---|---|---|---|
| 1 | 用户范围 | 内部 + 外部微信用户 | 必须双通道：自建应用 + 微信客服 |
| 2 | 业务 API | 已具备，**不可改造** | Bot 侧必须有 **API 适配层（ACL）** 做字段映射/参数补齐/单位换算/缓存 |
| 3 | 是否绑定账号 | 必须绑定 | 引入强绑定流程 + 数据级权限 |
| 4 | 多轮对话 | 需要，**跨天**，**多意图并存**（询价 / 订单 / 库存…） | 升级为 **Topic + Task 状态机** + 持久化会话 |
| 5 | 部署 | 公网云，域名已备案 | 走云原生（K8s / 云托管） |
| 6 | 行业 | 钢铁贸易 | 商业敏感（价格/客户/库存）→ 强权限 + DLP + 审计 |
| 7 | LLM | DeepSeek + 通义千问 | 引入 LLM Orchestrator + Function Calling + 双供应商路由 |

---

## 1. 需求与目标

### 1.1 用户与场景（钢铁贸易）

| 角色 | 典型诉求 |
|---|---|
| **下游客户**（建筑/机械/汽车/家电采购） | 询价、查行情、下单、查订单进度、查发货物流、要磅单/质保书、对账 |
| **销售** | 查自己客户的订单、查库存、查报价历史、查回款 |
| **采购** | 查上游钢厂价、查到货、查在途 |
| **仓管** | 查库存、出入库登记查询 |
| **财务** | 对账、回款查询 |
| **管理层** | 业绩、库存周转、风险预警 |

> 一次完整业务可能跨多天：客户周一询价 → 周二还在比价 → 周三下单 → 周四查发货 → 下周对账。会话必须能连续。

### 1.2 功能需求

- 双通道接入：企业微信内部员工（自建应用） + 普通微信用户（微信客服）。
- 自然语言对话（LLM 理解） + 关键字快捷命令并存。
- 多意图、多轮、长周期会话（跨天）。
- 调用现有业务 API 查询/写入数据（询价、订单、库存、物流、对账等）。
- 强绑定（必须先绑定才能查数据） + 数据级权限。
- 全链路审计与 DLP。

### 1.3 非功能需求

| 项 | 目标 |
|---|---|
| 回调响应延迟 | < 1 s（硬限 < 5 s） |
| LLM 回复整体延迟 | P50 < 3 s, P95 < 8 s（含 LLM + Function Call） |
| 命令快路径延迟 | P95 < 2 s |
| 可用性 | 99.5%+ |
| 并发 | 200 QPS 回调 / 50 QPS LLM |
| 安全 | HTTPS + 签名 + AES + KMS 密钥 + DLP |
| 合规 | 钢铁贸易非强监管，但商业敏感：日志 180d、操作审计 1y |

---

## 2. 通道选型

| 形态 | 接收 | 发送 | 用户 | 选用 |
|---|---|---|---|---|
| 群机器人 Webhook | ❌ | 推到群 | 群成员 | 仅用于内部告警，不作为主通道 |
| **自建应用** | ✅ | ✅ | 企业内部员工 | ✅ |
| **微信客服 (Kf)** | ✅ | ✅ | **普通微信用户** | ✅ |

> 严禁走个人微信号协议（itchat / wechaty 类），违反协议、封号风险高。"普通微信里使用"的唯一合规答案是**微信客服**。

---

## 3. 总体架构（v2）

```
                    ┌──────────────────────────────────────┐
   普通微信用户  ──▶│  微信客服 (kf_*)                       │
                    │  托管在企业微信里                      │
   企业微信员工  ──▶│  自建应用                              │
                    └────────────────┬─────────────────────┘
                                     │ HTTP POST 回调 (加密)
                                     ▼
   ┌────────────────────────────────────────────────────────────────┐
   │                       Bot Gateway                                │
   │ ┌────────────────────────────────────────────────────────────┐ │
   │ │  Callback Server: 验签 + AES 解密 + URL 校验                │ │
   │ └─────────────────────────────┬──────────────────────────────┘ │
   │                               ▼                                  │
   │ ┌────────────────────────────────────────────────────────────┐ │
   │ │  Channel Adapter (App / Kf 归一化为内部 Message)             │ │
   │ └─────────────────────────────┬──────────────────────────────┘ │
   │                               ▼                                  │
   │ ┌────────────────────────────────────────────────────────────┐ │
   │ │  Identity & Binding Manager                                 │ │
   │ │   - 检查 WeCom user / external_userid 是否已绑定           │ │
   │ │   - 未绑定 → 引导绑定流程                                  │ │
   │ │   - 已绑定 → 注入业务身份 (biz_user_id / role / tenant)    │ │
   │ └─────────────────────────────┬──────────────────────────────┘ │
   │                               ▼                                  │
   │ ┌────────────────────────────────────────────────────────────┐ │
   │ │  Permission Gate (命令级 + 数据级)                          │ │
   │ └─────────────────────────────┬──────────────────────────────┘ │
   │                               ▼                                  │
   │ ┌────────────────────────────────────────────────────────────┐ │
   │ │  Fast Path Router                                            │ │
   │ │   命中关键字命令 (/订单 123 / 行情 螺纹 / help)            │ │
   │ │   → 直接走 Command Handler，跳过 LLM                       │ │
   │ └──────────────┬─────────────────────────────────┬───────────┘ │
   │                │ 未命中                              │ 命中       │
   │                ▼                                    │           │
   │ ┌────────────────────────────────────────────────┐ │           │
   │ │  Session State Manager                          │ │           │
   │ │   Topic + Task 状态机 (跨天持久化)              │ │           │
   │ └──────────────┬──────────────────────────────────┘ │           │
   │                ▼                                    │           │
   │ ┌────────────────────────────────────────────────┐ │           │
   │ │  LLM Orchestrator                               │ │           │
   │ │   - 路由 (DeepSeek / Qwen)                      │ │           │
   │ │   - 意图识别 + 槽位填充                          │ │           │
   │ │   - Function Calling 决定调哪些工具              │ │           │
   │ │   - DLP 前/后置过滤                             │ │           │
   │ └──────────────┬──────────────────────────────────┘ │           │
   │                ▼                                    ▼           │
   │ ┌────────────────────────────────────────────────────────────┐ │
   │ │  Tool Registry (Function Calling 工具集)                    │ │
   │ │   query_price / quote / place_order / get_order /          │ │
   │ │   list_stock / get_logistics / get_weight_note / ...        │ │
   │ └─────────────────────────────┬──────────────────────────────┘ │
   │                               ▼                                  │
   │ ┌────────────────────────────────────────────────────────────┐ │
   │ │  Business API Adapter (ACL, 防腐层)                          │ │
   │ │   - 字段/单位/日期映射                                       │ │
   │ │   - 参数补齐 (从会话/绑定/默认值)                            │ │
   │ │   - 缓存 + 重试 + 熔断                                       │ │
   │ │   - 鉴权 (服务间 token / HMAC)                              │ │
   │ └─────────────────────────────┬──────────────────────────────┘ │
   │                               ▼                                  │
   │                          现有业务 API                            │
   │                                                                  │
   │ ┌────────────────────────────────────────────────────────────┐ │
   │ │  Reply Composer (Text / Markdown / TextCard)                │ │
   │ │   + 二次 DLP 检查 (禁止泄漏敏感字段)                         │ │
   │ └─────────────────────────────┬──────────────────────────────┘ │
   │                               ▼                                  │
   │ ┌────────────────────────────────────────────────────────────┐ │
   │ │  WeCom API Client (双 token / 主动发 + 客服发)              │ │
   │ └────────────────────────────────────────────────────────────┘ │
   └────────────────────────────────────────────────────────────────┘

   ┌──────────┐  ┌──────────┐  ┌──────────────┐  ┌────────────────┐
   │  Redis   │  │ Postgres │  │ Async Worker │  │ Audit / DLP    │
   │  token   │  │ 绑定/会话│  │ 慢任务/LLM   │  │ ES / OSS / SLS │
   │  ctx缓存 │  │ 工具日志 │  │ 工具调用     │  │ Prompt 留存    │
   │  限流    │  │ 审计     │  │ 主动推送     │  │                │
   └──────────┘  └──────────┘  └──────────────┘  └────────────────┘
```

---

## 4. 关键流程

### 4.1 端到端消息流（已绑定 + 自然语言）

```
用户            WeCom/微信       Gateway        Session       LLM         ACL         业务API
"螺纹钢HRB400  
 25mm 200吨
 送到武汉
 报个价"
 ───────────▶│
             │ POST 回调(加密) │              │             │            │           │
             │ ──────────────▶│              │             │            │           │
             │                │ 验签+解密     │             │            │           │
             │                │ 绑定/权限OK  │             │            │           │
             │                │ ─────────────▶ 取/建Topic  │            │           │
             │                │              │ ─────────────▶ 意图=询价 │           │
             │                │              │             │ 槽位:品种,规格,数量,目的地│
             │                │              │             │ Function: query_price ─▶│
             │                │              │             │            │ ──API ───▶│
             │                │              │             │            │ ◀─JSON ───│
             │                │              │             │            │ 映射回标准 │
             │                │              │ ◀───────────│ 组装答案    │           │
             │                │ ◀────────────│ 持久化Topic │            │           │
             │                │ 调发送消息API │              │            │           │
             │ ◀──────────────│              │             │            │           │
"螺纹钢HRB400 25mm 200吨
武汉到货价：¥3,820/吨
含税含运，48h 内有效
是否进入下单？回复『下单』继续"
```

### 4.2 多意图切换（用户在询价中插入查订单）

```
用户 → "螺纹钢 HRB400 25mm 200吨 武汉 报价"     ← 建立 Topic#A: 询价 (slot半填)
Bot  → "¥3,820/吨..."
用户 → "对了，上周下的那个 HRB335 16mm 的订单到哪了？"   ← 新意图: 查订单
       │
       ▼
Session Manager 检测到意图切换:
  - 暂存 Topic#A 状态 (status=paused)
  - 开 Topic#B: 查订单
  - 槽位不全 (没订单号) → 用绑定身份反查"最近的订单" + 模糊匹配规格
Bot  → "您的订单 SO2026-0512 已发车，预计明早 9 点到武汉江夏仓"
Bot  → "（询价还在继续）刚才螺纹钢 HRB400 25mm 200吨 的报价是否要锁价或下单？"
       ← 自动恢复 Topic#A
```

### 4.3 跨天会话恢复

```
Day1 21:30  用户询价 → Topic#A paused（夜间未确认）
Day2 09:15  用户："还是昨天那批 螺纹钢 200 吨，价格还有效吗？"
            ↓
Session Manager:
  - 查 Postgres 该用户最近 7 天活跃 Topic
  - 发现 Topic#A (询价) 仍在 paused
  - 把 Topic#A 注入 LLM 上下文
LLM:
  - Function Call: query_price(同 slot) → 取新价
  - 对比昨日报价 → 输出涨跌
Bot  → "今早行情已变，HRB400 25mm 武汉到货价 ¥3,795/吨（↓25），是否锁价？"
```

---

## 5. 模块设计（v2 新增/变更）

### 5.1 Session State Manager（新增/升级）

**模型**：

```
WeComUser ──1:1── BizUser
BizUser   ──1:N── Session       (一个会话主线，按用户+渠道)
Session   ──1:N── Topic         (一个话题，如"询价 螺纹钢 200吨")
Topic     ──1:N── Task          (具体动作，如"获取报价"、"锁价")
Topic     ──1:N── MessageTurn   (历史消息)
```

**字段（Topic）**：

| 字段 | 说明 |
|---|---|
| topic_id | UUID |
| intent | quote / order / stock / logistics / weight_note / settlement / ... |
| slots | JSON，槽位（品种、规格、数量、目的地、订单号…） |
| status | active / paused / completed / cancelled |
| ttl | 默认 7 天，可按意图调整（询价 48h，订单查询 30 天） |
| last_active_at | 用于过期清理 |
| ref_ids | 关联的业务 ID（订单号/报价单号） |

**关键能力**：

- **Topic 自动暂停/恢复**：检测到新意图 → 暂存当前 Topic → 处理新 Topic → 完成后提示"是否回到上一个询价"。
- **Topic 列表查询**：用户发"我有几件没办完？"→ Bot 列出 active/paused 的 Topic。
- **显式控制命令**：`/topics`、`/close 询价`、`/clear`。
- **上下文压缩**：长 Topic 的历史消息超过阈值 → 总结成"摘要 + 关键槽位"喂 LLM，避免 token 爆炸。
- **存储**：
  - 热数据（最近 48h）：Redis Hash，便于快速读
  - 冷数据：Postgres，便于跨天恢复

### 5.2 LLM Orchestrator（新增）

**职责**：

1. **意图分类**：把用户消息分到一个或多个意图 (`询价 / 订单 / 库存 / 物流 / 磅单 / 质保书 / 对账 / 客户管理 / 行情 / 闲聊 / 帮助`)。
2. **槽位填充**：从消息 + Topic 上下文里抽取槽位（品种、规格、数量、目的地、日期…）。
3. **Function Calling**：决定调哪个工具，组织参数。
4. **回答生成**：把工具返回的 JSON 渲染成符合钢铁贸易话术的回复。
5. **多模型路由**。

**模型路由策略**：

| 任务 | 首选 | 备选 | 理由 |
|---|---|---|---|
| 意图分类 / 槽位抽取 | Qwen-Turbo 或 DeepSeek-V3 | 互为备选 | 便宜快 |
| Function Calling 主链路 | DeepSeek-V3 | Qwen-Plus | 工具调用稳定 |
| 复杂推理 / 多约束询价归类 | DeepSeek-R1 | Qwen-Max | 推理能力 |
| 兜底闲聊 | Qwen-Turbo | DeepSeek-V3 | 便宜 |

**降级链**：

```
首选 LLM 超时/失败
  → 备选 LLM
  → 关键字命令兜底 + 引导话术
  → 最差："系统繁忙，请稍后或回复『人工』转销售"
```

**Prompt 工程要点**：

- System Prompt 固化 **「钢铁贸易助手」人设 + 业务术语词典**（HRB400/HRB500、Q235、SS400、热轧/冷轧/镀锌、卷板/中厚板/型钢、过磅、质保书、磅差、汽运/铁路/水运…）。
- 工具描述用 OpenAI Function Schema 标准，DeepSeek 和 Qwen 都兼容。
- **价格/库存等关键数字一律来自工具返回，禁止 LLM 自己编**。Prompt 里明确："禁止虚构价格、库存、订单号、客户名；不确定时调用工具或反问用户。"
- 输出格式约定（JSON 或固定字段），便于后置 DLP。

### 5.3 Tool Registry / Function Calling 工具集（新增）

为钢铁贸易定义最小可用工具集（每个工具 = 1~N 个业务 API 的封装）：

| 工具名 | 入参（关键） | 适用角色 | 备注 |
|---|---|---|---|
| `query_market_price` | 品种, 规格, 城市, 日期? | 全员 | 大盘行情，非成交价 |
| `query_quote` | 品种, 规格, 数量, 目的地 | 客户/销售 | 实际报价，含运 |
| `lock_quote` | quote_id, 有效期 | 客户/销售 | 锁价 |
| `place_order` | quote_id, 收货信息 | 客户/销售 | 下单（写操作，二次确认） |
| `query_order` | order_id? / 关键词 | 客户/销售/采购 | 不传 id 时按上下文 |
| `query_order_progress` | order_id | 全员 | 进度链：已下单→已生产→已出库→在途→已签收 |
| `list_stock` | 品种, 规格, 仓库? | 销售/采购/仓管 | 可用库存 |
| `get_logistics` | order_id / 车牌 | 全员 | 物流位置+预计到达 |
| `get_weight_note` | order_id / 车次 | 客户/销售 | 磅单 PDF/图片 |
| `get_quality_cert` | 炉批号 / order_id | 客户/销售 | 质保书 PDF |
| `query_settlement` | 客户编号, 月份 | 客户/销售/财务 | 对账单 |
| `query_receivable` | 客户编号 | 销售/财务 | 应收/账期 |
| `list_my_topics` | — | 全员 | 列我的未办事项 |
| `escalate_to_human` | 原因 | 全员 | 转人工销售/客服 |

**工具描述示例**（喂给 LLM 的 schema）：

```json
{
  "name": "query_quote",
  "description": "查询/生成一份钢材报价。需要齐全的钢材品种、规格、数量、目的地；缺失时请向用户反问。",
  "parameters": {
    "type": "object",
    "properties": {
      "category":     {"type":"string", "description":"品种，如 螺纹钢/盘螺/线材/热轧卷/冷轧卷/中厚板/H型钢"},
      "grade":        {"type":"string", "description":"牌号，如 HRB400/HRB500/Q235/SS400"},
      "spec":         {"type":"string", "description":"规格，如 25mm、6.0*1500*C"},
      "qty_ton":      {"type":"number", "description":"数量（吨）"},
      "dest_city":    {"type":"string", "description":"交货城市/仓库"},
      "delivery_date":{"type":"string", "description":"期望交货日期 YYYY-MM-DD，可空"}
    },
    "required": ["category","grade","spec","qty_ton","dest_city"]
  }
}
```

**写操作（place_order / lock_quote）必须二次确认**：LLM 返回工具调用后，Bot 先把"将下单 XXX，请回复『确认』"发给用户，**用户确认才真正调 API**。

### 5.4 Business API Adapter（ACL，新增/重点）

> 因为业务 API 不能改造，这层就是**整个系统的工程重心**。

职责：

1. **字段映射**：业务 API 用 `productCode/specCode` → 内部统一为 `category/grade/spec`。
2. **枚举翻译**：业务 API 的状态码 `01/02/03` ↔ "已下单/已生产/在途"。
3. **单位换算**：吨 ↔ 千克、米 ↔ 毫米。
4. **参数补齐**：用户没说的从（绑定关系 → 当前 Topic → 用户默认偏好 → 系统默认值）逐级补全。
5. **缓存**：
   - 行情/库存：30~60 秒
   - 客户档案/合同：5 分钟
   - 磅单/质保书 URL：不缓存
6. **重试 + 熔断 + 超时**：每个下游 API 独立配置。
7. **鉴权**：服务间 token / HMAC，Bot 持服务账号身份调 API；**数据级权限的过滤逻辑**也在这里加一道兜底（即便业务 API 没限制，Bot 也只放符合用户身份的数据出去）。
8. **可观测**：每次调用打 trace_id + biz_user_id + tool_name + latency + errcode。

### 5.5 Identity & Binding Manager（强化）

**绑定方式**（钢铁贸易适用，二选一或并存）：

| 方式 | 流程 | 适用 |
|---|---|---|
| **一次性 Token** | 销售/管理员在业务系统给客户/员工生成 6 位 token（10 分钟有效）→ 用户发"绑定 ABC123" | 客户、员工 |
| **手机号 + 短信验证码** | 用户发"绑定" → Bot 反问手机号 → 业务系统下发短信 → 用户回填验证码 | 客户自助 |
| **企业微信通讯录自动绑** | 内部员工通过企业微信 UserID 直接对照公司 HR 系统 | 仅内部员工 |

**绑定后注入的身份**：

```
biz_identity = {
  biz_user_id,
  role: customer | sales | purchaser | warehouse | finance | admin,
  customer_id?: <如果是客户>,
  managed_customer_ids?: <销售管的客户>,
  warehouse_ids?: <仓管负责的仓库>,
  tenant_id,
}
```

**多身份切换**（销售常见）：

- 销售可能同时是某客户的负责人 + 公司员工 → 默认员工视角，可发"切换身份 客户视角"。
- Topic 内身份固定，避免数据穿越。

### 5.6 Permission Gate（强化）

**两层权限**：

1. **命令级**：每个工具声明允许的角色集合。客户不能调 `query_receivable`（应收）、不能看其它客户的订单。
2. **数据级**：在 ACL 层根据 `biz_identity` 过滤：
   - 客户：只看 `customer_id == biz_identity.customer_id` 的数据
   - 销售：只看 `customer_id IN managed_customer_ids` 的数据
   - 仓管：只看自己仓库的库存
   - 管理员：全量

**默认拒绝**：未声明授权的工具 = 拒绝。

### 5.7 DLP & Audit（强化）

**DLP 双道**：

- **前置（入 LLM 前）**：脱敏 prompt 里的成本价、毛利率、内部备注，不让 LLM "见到"。
- **后置（出 LLM 后 / 发用户前）**：扫敏感字段，命中即重写/拒答。
  - 敏感字段清单：`cost_price / profit_margin / internal_remark / customer_credit_limit / other_customer_*`
  - 客户视角下，"其他客户的价格"绝不能出现。

**审计**：

- 所有消息进出全量日志 → ES/SLS（180 天）。
- 所有工具调用：`who / when / tool / params / result_hash / latency` → 单独审计表（1 年）。
- LLM Prompt/Response 留存（OSS，加密）：便于复盘和事后追责。
- 敏感操作（下单、改价、转人工）→ 单独审计 + 可选通知销售。

### 5.8 WeCom API Client / Async Worker / Storage

保留 v1 设计，补充：

- **微信客服**：增加"客服会话状态轮询"，区分新会话 / 老会话；处理"48h 窗口"友好提示。
- **Async Worker**：增加 LLM 调用专用队列（隔离慢任务），失败重试 ≤ 2 次。
- **Storage** 新表：

| 表 | 用途 |
|---|---|
| `user_binding` | 微信身份 ↔ 业务身份 |
| `session` / `topic` / `topic_message` | 会话状态机 |
| `tool_call_log` | 工具调用审计 |
| `llm_call_log` | LLM 调用审计（含 prompt/response OSS 引用） |
| `permission_matrix` | 角色 × 工具的允许矩阵（可后台维护） |

---

## 6. 钢铁贸易场景的话术与体验（v2 新增）

- **行业语黑话兼容**：用户可能说"螺四 25 三百吨送武汉"，LLM 需识别"螺四=HRB400 螺纹钢，25=25mm，三百吨=300t"。在 system prompt 里给出别名词典。
- **报价话术**：必含「品种 + 牌号 + 规格 + 数量 + 目的地 + 单价（含税含运/出厂）+ 有效期 + 是否锁价/下单引导」。
- **进度可视化**：进度链用 Markdown 横线连贯，比如 `下单 ✅ → 生产 ✅ → 出库 ✅ → 在途 🚚 → 签收 ⏳`。
- **磅单/质保书**：以"图片消息 + 文字摘要"双发；客户视角下隐藏内部检验单字段。
- **写操作（下单、锁价、改地址）**：一律先回显参数 + 二次确认 + 留下"反悔窗口 60s 可撤销"。
- **行情**：每日 8:30 主动推送订阅用户当日行情卡片（注意微信客服 48h 窗口，超窗用户先发起会话才能推）。
- **多人协作**：群里 @ 机器人 → 在群里回复；但敏感数据（价格/对账）不进群，引导私聊。

---

## 7. 安全与合规（钢铁贸易适配）

| 项 | 措施 |
|---|---|
| 密钥 | 全部走云上 KMS / 环境变量，禁止入库 |
| 通道 | HTTPS 强制 + 回调签名 + AES-CBC |
| 业务 API 调用 | 服务间 Token / HMAC + IP 白名单 |
| 数据脱敏 | DLP 前置 + 后置；身份证/银行卡正则脱敏 |
| LLM 数据出域 | DeepSeek / Qwen 公有 API → prompt 不带成本/毛利；如客户有要求可后期切私有化 |
| 操作审计 | 所有读操作 INFO 级日志；所有写操作单独审计表 + 不可篡改（追加写） |
| 数据保留 | 消息 180 天；审计 1 年；财务相关 5 年 |
| 越权防护 | Permission Gate 默认拒绝 + ACL 二次过滤 |
| 滥用防护 | 单用户限流（30 条/分钟）、单用户 LLM token 配额、敏感命令二次确认 |

---

## 8. 可观测性（v2 强化）

- 指标：
  - `wecom_callback_total{channel,result}`
  - `llm_call_total{provider,model,result}`、`llm_tokens_total{provider}`、`llm_latency_seconds`
  - `tool_call_total{tool,result}`、`tool_latency_seconds`
  - `biz_api_latency_seconds{endpoint}`
  - `session_active_topics{intent}`
- 告警：
  - LLM 失败率 > 5%（5min）→ 自动切备用供应商
  - 单用户 token 异常飙升 → 风控告警
  - 业务 API 5xx > 1%
  - 客服 48h 窗口外推送失败堆积

---

## 9. 部署拓扑（公网云）

```
   Internet ──▶ WAF/CDN ──▶ SLB (HTTPS) ──▶ Nginx Ingress
                                                │
                          ┌─────────────────────┼────────────────────┐
                          ▼                     ▼                    ▼
                   Bot Gateway (HPA)     Async Worker (HPA)     Cron (订阅推送)
                          │                     │                    │
                          ├──── Redis (托管) ───┤                    │
                          ├──── Postgres (托管) ┤                    │
                          ├──── OSS (Prompt/磅单缓存) ───────────────┤
                          ├──── KMS (密钥) ─────┤                    │
                          └──── SLS/ES (日志/审计) ───────────────────┘
                          │
                          ▼
                  DeepSeek API / 通义千问 API
                          │
                          ▼
                  现有业务 API（内网/VPC 对等）
```

要点：

- 优先用**云托管**（K8s/容器服务 + 托管 Redis/Postgres），降低运维成本。
- 业务 API 在内网 → 走 **VPC 对等 / 内网域名**，不暴露公网。
- LLM 出公网走 **NAT 网关 + 出口白名单**。
- Secrets 走 **KMS / Secrets Manager**，启动时注入。

---

## 10. 里程碑（v2，仅按交付物，不估时间）

| 里程碑 | 交付物 |
|---|---|
| **M1 基础设施** | 域名/证书、企业微信后台建应用 + 客服、KMS 密钥、云资源 |
| **M2 通信骨架** | 双通道回调验签/解密通过；access_token 缓存可用；自建应用能 echo |
| **M3 绑定 + 权限** | 一次性 token 绑定打通；Permission Gate v1（命令级） |
| **M4 业务 ACL** | 1~2 个核心工具（query_order / list_stock）打通；缓存+重试+审计 |
| **M5 LLM 编排** | DeepSeek + Qwen 双路由；Function Calling 跑通 query_order；降级链可用 |
| **M6 会话状态机** | Topic + Task 模型；Redis+Postgres 双层；意图切换 + 跨天恢复 |
| **M7 钢铁贸易技能扩展** | 询价/报价/锁价/下单（含二次确认）/物流/磅单/质保书/对账 |
| **M8 微信客服** | Kf 通道接入，普通微信用户可对话；48h 窗口策略 |
| **M9 DLP + 数据级权限** | 前/后置 DLP；数据级过滤；审计闭环 |
| **M10 可观测 + 风控** | 指标/告警/限流/token 配额 |
| **M11 主动推送** | 每日行情订阅推送；订单状态变更主动通知 |
| **M12 灰度上线** | 内部员工 → 部分客户 → 全量；A/B LLM 供应商 |

---

## 11. 风险与对策（v2 更新）

| 风险 | 影响 | 对策 |
|---|---|---|
| LLM 幻觉报错价 | 商业损失/客诉 | 价格/库存/订单号一律工具返回；prompt 严禁编造；后置 DLP 校验数字字段必须来自工具 |
| 价格/客户信息泄漏到错的人 | 严重商业损失 | 双层权限 + DLP；客户视角默认最小可见 |
| LLM 供应商故障/限流 | 全员不可用 | 双供应商热切换 + 关键字命令兜底 |
| LLM token 成本失控 | 财务压力 | 关键字快路径 + 上下文压缩 + 单用户/日配额 |
| 业务 API 不稳定 | 用户体验差 | ACL 层重试 + 熔断 + 缓存；失败时给"暂不可用 + 转人工" |
| 微信客服 48h 窗口 | 主动推送失效 | 引导用户先发起会话；申请 `send_msg_on_event` |
| 跨天 Topic 太多 → ctx 爆炸 | LLM 报错 | 摘要压缩 + 自动归档已完成 Topic |
| 写操作误触发（误下单） | 资损 | 二次确认 + 60s 反悔窗 + 大额订单转人工 |
| 个人微信号自动化诱惑 | 封号 | 严格禁止；只走微信客服 |
| 密钥泄漏 | 企业微信能被冒用 | KMS + 严格审计 + 定期轮转 |

---

## 12. 后续演进

- **RAG 知识库**：把公司价格政策、客户白名单、钢材标准（GB/T 1499.2 等）做向量检索，喂 LLM。
- **多模态**：识别用户发来的磅单/合同照片（OCR + LLM 抽取）。
- **主动智能**：基于客户历史行为做"行情下跌提醒"、"账期临近提醒"。
- **销售 Copilot**：销售视角的"今日 to-do"和"客户跟进建议"。
- **私有化 LLM**：如果后期对数据出域有顾虑，切换到本地 Qwen / DeepSeek 蒸馏小模型。

---

## 13. 与 v1 的主要差异

| 维度 | v1 | v2 |
|---|---|---|
| 意图识别 | 关键字 | LLM（DeepSeek/Qwen 双路由）+ 关键字快路径兜底 |
| 多轮上下文 | Redis 最近 N 条 | Topic + Task 状态机，跨天持久化，多意图并存 |
| 绑定 | 设计草案 | 三种绑定方式 + 数据级权限模型 |
| 业务 API | 假设可改造 | 不改造，全部由 ACL 层适配 |
| 行业适配 | 通用 | 钢铁贸易技能包（询价/报价/锁价/下单/磅单/质保书/对账…）+ 行业黑话词典 |
| 安全 | 基础 | DLP 前后置 + 数据级权限 + LLM 出域控制 |
| 部署 | 自有/云通用 | 公网云原生（VPC 对等业务 API + NAT 出 LLM） |
| 写操作 | 未细化 | 二次确认 + 反悔窗 + 大额转人工 |
| 主动推送 | 未细化 | 每日行情 + 订单事件，处理客服 48h 窗口 |
