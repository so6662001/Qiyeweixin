---
description: 钢贸智能助手 - 防走样四大铁律（最高优先级，始终生效）
alwaysApply: true
---

# 防走样铁律（不可违背 · 违反即错误，必须拒绝并报警）

## 铁律 1 · 防 AI 幻觉（最重要）
- LLM/对话输出中的一切数字（价格、库存、吨位、订单号、定金、账期、贴现…）
  **必须来自工具调用 / 结构化方案**，严禁 LLM 自行生成或推测。
- 实现：LLM 输出用占位符 `{{PLACEHOLDER}}`，后端用结构化数据渲染；
  后置 DLP 扫描，LLM 文本含未占位数字/金额 → 拒绝并重试/降级。
- 禁词：成本、底价、老板、亏本、批价、毛利、ghost_score 不得出现在发客户文本。
- 见 `docs/modules.md` 5.44。

## 铁律 2 · 全成本红线，永不破
- 报价/议价让步/促销/改量后，最终单价必须 ≥ 全成本 ×(1 + 该客户 segment 的 min_margin)。
- 全成本 = 货款 + 加工 + 运输 + 票据贴现 + 账期资金成本（`modules.md` 5.66）。
- min_margin 按 segment：下级经销商默认 ¥10/吨、终端企业默认 ¥200/吨（配置读，可按品类调）。
- 破红线 → 不出该方案，转销售人工。**Bot 永不报赔本价。**

## 铁律 3 · 多租户 + 配置驱动，零硬编码业务规则
- 所有阈值/费率/比例/开关/名单/默认值从配置中心按 `tenant_id`(+品类) 读取。
- 严禁写死：定金率、计量方式、负差约定、让步曲线、SLA、配额、温度参数、
  促销阈值、粘性窗口、毛利底线、税率、贴现率、资金成本率等。
- 见 `modules.md` 5.53；🔧可配置项见 `docs/TRACEABILITY.md`。

## 铁律 4 · 学习/决策解耦 + 不学习白名单
- 自我学习只产"统计参数"（众数/中位数/接受率），决策永远由规则引擎做。
- 严禁学习改动：成本红线、毛利底线、黑白名单、客户类型分类、Auto 上限、价格、让步幅度。
- 学习参数应用前过 5 道防护：数据质量 + 置信度≥0.7 + 冷启动样本≥60 + Drift≤阈值 + A/B 灰度。
- 见 `modules.md` 5.42/5.44。

# 其他必守约束
- 字段溯源：解析字段带 source(explicit/inferred/calculated/user_confirmed/missing)+confidence；critical 缺失必反问，不静默默认。
- 微信边界：不渲染 Markdown（纯文本空格对齐）；回调 5s 内 ACK（慢任务异步）；微信客服 48h 主动消息窗口；消息类型按官方限制。
- 幂等去重：回调按 MsgId 去重(Redis setnx 5min)；业务 webhook 按 event_id 幂等。
- 库存安全：留货/下单分布式锁(按 sku/批次)，与 ERP 分钟级对账，绝不超卖；批次价同 FIFO。
- 数据级权限：客户只见自己数据；销售只见 managed_customers；成本/毛利/画像/ghost 绝不出客户消息。
- 写操作二次确认：下单/锁价/留货/改规格/替代 必须客户明确确认；锁价后改量按 ±5%/累计±10% 审批。
- access_token：集中缓存 + 分布式锁 + 提前续期；自建应用与客服 token 两套独立。
- 降级链：LLM→备选→关键字→人工；VL→Plus→人工；ASR 低置信→回显；H5→菜单→纯文字。
- 审计：报价/议价/改量/留货/替代/转人工/配置变更/学习应用 全留审计。
